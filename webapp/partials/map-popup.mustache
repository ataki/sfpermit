<div class="popup">
    <h3><strong>{{project_name}}</strong></h3>
    <div><i class="fa fa-map-marker"></i> <span class="text-gray">{{address}}</span></div>
    <p> 
    {{#Open}}
        This permit has a <span class="text-{{prediction_level}}">{{prediction}} chance of approval</span>
    {{/Open}}
    {{^Open}}
        {{#Cancelled}}
        This permit's status is <span class="label label-default">{{final_status}}</span>
        {{/Cancelled}}
        {{#Approved}}
        This permit's status is <span class="label label-success">{{final_status}}</span>
        {{/Approved}}
    {{/Open}}
    {{#days}}
    Active for <strong><span class="text-{{days_level}}">{{days}}</span> days</strong>
    {{/days}}
    {{^days}}
    Active for an <i>unknown</i> number of days
    {{/days}}, has <b>{{net_units}}</b> net units.<br/>
    </p>
    <p>
        <a href="/edit/{{id}}" class="btn" target="_blank">Edit</a>
        <a href="#" class="btn">View Detail</a>
    </p>
</div>